# 🚀 Railway 部署指南

本文档说明如何将 PGC 生物实验室项目部署到 Railway 平台。

## 📋 前置要求

- GitHub 账户
- Railway 账户（可使用 GitHub 登录）
- 项目已推送到 GitHub 仓库

## 🔧 部署步骤

### 方法一：通过 Railway Dashboard（推荐）

1. **登录 Railway**
   - 访问 [railway.app](https://railway.app)
   - 使用 GitHub 账户登录

2. **创建新项目**
   - 点击 "New Project"
   - 选择 "Deploy from GitHub repo"

3. **选择仓库**
   - 授权 Railway 访问你的 GitHub
   - 选择 `pgc-bio-lab` 仓库
   - 点击 "Deploy Now"

4. **自动部署**
   - Railway 会自动检测 `railway.json` 配置
   - 自动检测 `package.json` 并安装依赖（如果有）
   - 执行 `node server.js` 启动服务

5. **获取 URL**
   - 部署完成后，点击 "Generate Domain"
   - Railway 会分配一个公开访问的 URL
   - 例如：`https://pgc-bio-lab-production.up.railway.app`

### 方法二：通过 Railway CLI

1. **安装 Railway CLI**
   ```bash
   npm install -g @railway/cli
   ```

2. **登录 Railway**
   ```bash
   railway login
   ```

3. **初始化项目**
   ```bash
   cd pgc-bio-lab
   railway init
   ```

4. **部署项目**
   ```bash
   railway up
   ```

5. **打开应用**
   ```bash
   railway open
   ```

## ⚙️ 配置说明

### railway.json
```json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "node server.js",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 5
  }
}
```

### 环境变量（可选）

Railway 会自动设置以下环境变量：
- `PORT`: Railway 分配的端口（自动）
- `NODE_ENV`: 设置为 `production`（可选）

如需自定义环境变量：
1. 在 Railway Dashboard 中选择项目
2. 进入 "Variables" 标签
3. 添加自定义变量

## 🔍 验证部署

部署成功后，访问分配的 URL，你应该看到：
- PGC 生物实验室的完整界面
- 可以选择基因特性
- 可以生成和查看 3D 生物
- 所有动画和特效正常工作

## 🐛 故障排查

### 问题：部署失败

**解决方案**：
1. 检查 Railway 日志（Logs 标签）
2. 确认 `server.js` 文件存在
3. 确认 `package.json` 格式正确

### 问题：页面无法加载

**解决方案**：
1. 检查 `server.js` 是否正确监听 `PORT` 环境变量
2. 确认所有静态文件路径正确
3. 查看浏览器控制台错误信息

### 问题：Three.js 无法加载

**解决方案**：
1. 检查 CDN 链接是否可访问
2. 确认 `index.html` 中的 script 标签正确
3. 查看网络请求是否被 CORS 阻止

### 问题：模块加载失败

**解决方案**：
1. 确认所有 `.js` 文件使用正确的相对路径
2. 检查 `server.js` 的 MIME 类型设置
3. 确认浏览器支持 ES6 模块

## 📊 性能优化

### 启用缓存
服务器已配置静态资源缓存：
- HTML 文件：无缓存（实时更新）
- JS/CSS/图片：1 年缓存

### CDN 加速
Three.js 使用 unpkg CDN，全球加速访问。

### 压缩传输
Railway 自动启用 Gzip 压缩。

## 🔄 持续部署

Railway 支持自动部署：
1. 每次推送到 GitHub 的 `master` 分支
2. Railway 自动检测变更
3. 自动重新构建和部署
4. 零停机时间更新

## 💰 成本说明

Railway 提供：
- **免费计划**: 每月 $5 免费额度
- **按需付费**: 超出后按使用量计费
- 本项目预计每月成本：$0-2（低流量）

## 🔗 相关链接

- [Railway 官方文档](https://docs.railway.app)
- [Railway 定价](https://railway.app/pricing)
- [Railway 状态页](https://status.railway.app)

## 📝 更新日志

### 2024-01-03
- 初始部署配置
- 添加 Node.js 服务器
- 配置 railway.json

---

**部署完成后，记得在 README.md 中添加 Live Demo 链接！**
